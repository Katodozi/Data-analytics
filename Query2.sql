-- Calculate the total revenue generated from the pizza sales

-- (ctrl+B) to prettify the query
SELECT 
    ROUND(SUM(order_details.quantity * pizzas.price),
            2) AS total_sales
FROM
    order_details
        JOIN
    pizzas ON order_details.pizza_id = pizzas.pizza_id;